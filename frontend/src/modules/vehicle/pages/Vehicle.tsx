import VehicleLocation from "../components/VehicleLocation";

type Props = {};

const Vehicle = ({}: Props) => {
  return (
    <div className="h-screen">
      <VehicleLocation />
    </div>
  );
};

export default Vehicle;
